version: 2.1
jobs:
  test:
    docker:
      - image: python:3.8 # Specify the Python version
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install -r requirements.txt
      - run:
          name: Run tests
          command: python -m unittest discover
